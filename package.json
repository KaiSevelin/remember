{
  "name": "remember",
  "version": "1.0.0",
  "description": "A tiny single-page app for remembering things, with alarms for overdue notes",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "dev": "node --no-warnings --watch server.js --dev",
    "ds:bundle": "node --no-warnings tools/bundle-design-system.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
