# SQLite database — contains real accounts and password hashes, never commit it
remember.db
remember.db-shm
remember.db-wal
*.db
*.db-shm
*.db-wal

node_modules/
.env

# Generated by `npm run ds:bundle` — rebuild it, don't commit it
dist/

# OS cruft
Thumbs.db
.DS_Store
